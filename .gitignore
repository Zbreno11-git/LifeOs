.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/
.env
data/*
!data/.gitkeep

# credenciais locais reais (OAuth Google, etc.) — nunca commitar
secrets/*
!secrets/.gitkeep

# fork externo (repo git próprio, não faz parte do histórico deste projeto)
windows-mcp/

# clone externo (repo git próprio, origin aponta direto pro upstream browser-use/jev-ultrafast,
# não é um fork pessoal — não faz parte do histórico deste projeto); stack de automação de
# navegador (Jev + Browser Harness) — ver docs/fontes/browser-harness.md e docs/fontes/jev-typesafe.md
jev-ultrafast/

.DS_Store
