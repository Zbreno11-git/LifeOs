# LifeOs

Workspace Python 3.12.

## Setup

```bash
source .venv/bin/activate
pip install -e ".[dev]"
```

## Estrutura

- `src/lifeos/` — código da aplicação
- `tests/` — testes (pytest)
- `docs/` — documentação
- `data/` — dados locais (ignorado pelo git)
